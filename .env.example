# Copy to .env and adjust for your environment.
# Docker compose LAN default:
PGHOST=172.20.0.20
# Local dev without Docker:
# PGHOST=localhost
PGPORT=5432
PGDATABASE=network_logs
PGUSER=monitor
PGPASSWORD=monitor
PGCONNECT_TIMEOUT=3
